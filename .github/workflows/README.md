# Reusable workflows

Reusable workflow files will live here.

Caller repositories invoke them with:

```yaml
jobs:
  example:
    uses: placeholder-owner/placeholder-actions/.github/workflows/example.yml@v1
```
